{% extends 'base.html.twig' %}

{% block content %}

<div class="content">
    <div class="save-user__container">
       {{dump(userInfos)}}
        {{dump(allUsers)}}

        {{dump(emailExists)}}

         {{dump(user_manipulation_process)}}
       {{userInfos.firstname}}
    </div>
</div>

{% endblock %}
